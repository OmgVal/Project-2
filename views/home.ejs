<h2> Trending 🥳</h2>

<div>
    <a href="/gifs/">Search</a>
</div>

<div>
    <% gifs.forEach(gif => { %>
        <a href="/gifs/details/<%= gif.id %>">
            <% if (gif.images.original.url != " ") { %>
            <img  src="<%= gif.images.original.url %>" alt="<%= gif.title %>"/> 
            <% } %>
        </a>
    <% }) %>
</div>
