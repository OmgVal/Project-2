<h2> Trending 🥳</h2>



<div>
    <% gifs.forEach(gif => { %>
        <% if (gif.images.original.url != " ") { %>
        <img  src="<%= gif.images.original.url %>" alt="<%= gif.title %>"/> 
        <% } %>
    <% }) %>
</div>

